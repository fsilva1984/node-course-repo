
import fnSoma from "./moduleSoma.mjs";
import aritmetic from "./moduleAritmetic.mjs"

fnSoma(52, 36)

const { soma, sub, mult, pow, div, root } = aritmetic;

let adicao = soma(7, 23)
let potencia = pow(9, 12)
let divisao = div(18, 6)

